[build]
builder = "nixpacks"

[deploy]
startCommand = "playwright install chromium --with-deps && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 180 --workers 2"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
